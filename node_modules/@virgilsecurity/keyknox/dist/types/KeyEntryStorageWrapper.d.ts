import { IKeyEntry, IKeyEntryStorage, ISaveKeyEntryParams, IUpdateKeyEntryParams } from 'virgil-sdk';
export default class KeyEntryStorageWrapper {
    private readonly prefix;
    private readonly prefixRegExp;
    private readonly keyEntryStorage;
    constructor(identity: string, keyEntryStorage: IKeyEntryStorage);
    save(params: ISaveKeyEntryParams): Promise<IKeyEntry>;
    load(name: string): Promise<IKeyEntry | null>;
    exists(name: string): Promise<boolean>;
    remove(name: string): Promise<boolean>;
    list(): Promise<IKeyEntry[]>;
    update(params: IUpdateKeyEntryParams): Promise<IKeyEntry>;
    clear(): Promise<void>;
    private getKeyEntryName;
    private formatKeyEntry;
    private getAllKeyEntries;
}
