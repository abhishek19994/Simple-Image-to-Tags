/// <reference types="node" />
import { AxiosInstance } from 'axios';
import { EncryptedKeyknoxValue, DecryptedKeyknoxValue } from '../entities';
import IKeyknoxClient from './IKeyknoxClient';
export default class KeyknoxClient implements IKeyknoxClient {
    private static readonly AUTHORIZATION_PREFIX;
    private readonly axios;
    constructor(apiUrl?: string, axiosInstance?: AxiosInstance);
    pushValue(meta: Buffer, value: Buffer, token: string, previousHash?: Buffer): Promise<EncryptedKeyknoxValue>;
    pullValue(token: string): Promise<EncryptedKeyknoxValue>;
    resetValue(token: string): Promise<DecryptedKeyknoxValue>;
    private static getKeyknoxValue;
    private static getAuthorizationHeader;
    private static responseErrorHandler;
}
